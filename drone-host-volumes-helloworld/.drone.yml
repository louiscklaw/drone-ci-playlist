kind: pipeline
type: docker
name: default

steps:
- name: build
  image: node
  volumes:
  - name: logic_home
    path: /tmp/logic_home
  commands:
  - pwd
  - ls -l /tmp/logic_home

volumes:
- name: logic_home
  host:
    path: /home/logic/_workspace