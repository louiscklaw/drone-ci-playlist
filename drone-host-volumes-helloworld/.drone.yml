kind: pipeline
type: docker
name: default

steps:
- name: build
  image: node
  volumes:
  - name: logic_home
    path: /tmp/home
  commands:
  - pwd
  - ls -l

volumes:
- name: logic_home
  host:
    path: /home/logic/_workspace