

# inserted by test hello-merger/.drone.yml start
kind: pipeline
name: hello-merger
type: docker

steps:
- name: hello-merger
  image: logickee/ubuntu-basic
  commands:
  - echo $DRONE_BRANCH

  - git clone https://$GITHUB_TOKEN@github.com/louiscklaw/drone-ci-playlist.git /tmp/test_git_dir

  - cd /tmp/test_git_dir
  -   git checkout $DRONE_BRANCH
  - cd -

  - cd hello-merger
  -   pipenv run python3 ./src/main.py /tmp/test_git_dir
  - cd -

  environment:
    GITHUB_TOKEN:
      from_secret: GITHUB_TOKEN

# ERROR check: linter: invalid or unknown pipeline dependency
# depends_on will update by scripts/build.sh
depends_on:
  - .config
  - .npm
  - helloworld-rpi
  - hello-pipenv
  - hello-build-stages
  - hello-slack
  - hello-nodejs
  - hello-secrets
  - helloworld
  - hello-failure



trigger:
  status:
  - success
  branch:
  - develop
  - test/*


---
kind: secret
name: GITHUB_TOKEN
data: KahdXW1UO0TePy8Fh5BQ0ghArK0X3JYtDP3aQMCyrt9fX0gYLWv8Qp1p4f4uzXt+mgKWXIQTVuH1RfvX7UwIOxNxWHI=

# inserted by test hello-merger/.drone.yml end